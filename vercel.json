{
  "$schema": "https://openapi.vercel.sh/vercel.json",
  "outputDirectory": "frontend/src/pages",
  "_comment_cleanUrls": "true = /produtos.html redireciona (308) para /produtos e a URL some com a extensão. Os links no HTML continuam com .html de propósito: assim o preview local (python -m http.server) segue funcionando; em produção a Vercel faz o redirect.",
  "cleanUrls": true,
  "trailingSlash": false,
  "functions": {
    "api/pagamento.js": { "maxDuration": 20 },
    "api/pix.js": { "maxDuration": 20 },
    "api/webhook-mp.js": { "maxDuration": 15 }
  }
}
